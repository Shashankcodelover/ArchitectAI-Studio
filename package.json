{
  "name": "architectai-studio-workspace",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build": "cd ai-architect-frontend && npm install && npm run build && node -e \"const fs=require('fs'); if(!fs.existsSync('../dist')) fs.mkdirSync('../dist'); fs.cpSync('dist', '../dist', {recursive: true});\"",
    "start": "cd ai-architect-backend && npm start"
  }
}